-- Tests can be run by running a http-server and open the /test/testrunner.html file.
---- Failures will be displayed in the console, and reflected as the background color.


Application can be run with npm start, and will by default be reachable at http://localhost:8765/

Due to time constrain possible, performance optimizations was left out.
